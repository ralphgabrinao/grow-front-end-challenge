import React from 'react';
import styled from 'styled-components';

const Wrapper = styled.div`
	padding: 20px;
	height: 150px;
`;

export const TransactionBreakdown = (props) => {
	const { data } = props;
	return (
		<Wrapper>
		</Wrapper>
	);
}